---
"@superlend/sdk": minor
"@superlend/react-sdk": minor
---

Add overview API (`getOverview`) to SDK and wire up OverviewWidget to display top APY and opportunity counts for lending and vault categories. Update `onAction` callback to use `WidgetOpportunity`/`WidgetCalldata` union types.
